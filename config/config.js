const host = 'localhost';
const user = 'root';
const port = 5000;
const password = 'root';
const database = 'inventoryManagement';

module.exports = {
    host,
    user,
    password,
    database,
    port
}